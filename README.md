Package with flaticon 
=====================

This a package for iconique. The bower contain the css and the fonts have to be include with a task in the prod building task.

To install it `bower --allow-root chadicon-flat --save-prod`